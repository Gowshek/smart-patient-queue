[build]
  base = "frontend/smart_queue_app"
  command = "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git .flutter && .flutter/bin/flutter config --enable-web && .flutter/bin/flutter build web --release"
  publish = "build/web"